#include<iostream>
#include <fstream>
using namespace std;
int x = number_of_vowels
int main(){
    ofstream Myfile("FileData.txt");
    if (Myfile.is_open()){
        My file<<"This is advanced computer programing.\n";
    }
    else{
        cout<<"unable to open file";
    }
   while (!(file is open))
    {
        int count = number_of_vowels;
         std::cout << "number_of_vowels is: " x;
         std::cin > x;
    }
    std::cout << "x is" number_of_vowels<< std::endl;

}